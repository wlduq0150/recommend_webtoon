export type SelectOption = {
    genreUpCount?: number,
    genreDownCount?: number,
    updateDay?: string,
    category?: string,
    service?: string,
    fanCount?: number,
    descriptionLength?: number;
}